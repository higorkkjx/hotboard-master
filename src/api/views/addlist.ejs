<!DOCTYPE html>
<html lang="pt-BR" class="dark">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Gerenciador de Listas de Contatos</title>
  <script src="https://cdn.tailwindcss.com"></script>
  <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0-beta3/css/all.min.css" rel="stylesheet">
  <script>
    tailwind.config = {
      darkMode: 'class',
      theme: {
        extend: {
          colors: {
            dark: {
              100: '#333333',
              200: '#2d2d2d',
              300: '#272727',
              400: '#1f1f1f',
              500: '#171717',
            }
          }
        }
      }
    }
  </script>
  <style>
    @import url('https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap');
    body {
      font-family: 'Poppins', sans-serif;
    }
  </style>
</head>
<body class="bg-dark-500 text-gray-200 min-h-screen flex flex-col">
  <nav class="bg-dark-400 shadow-lg">
    <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="flex items-center justify-between h-16">
        <div class="flex items-center">
          <i class="fas fa-address-book text-purple-500 text-2xl mr-2"></i>
          <span class="font-semibold text-xl">Gerenciador de Listas</span>
        </div>
        <div>
          <button id="themeToggle" class="text-gray-300 hover:bg-dark-300 hover:text-white px-3 py-2 rounded-md text-sm font-medium">
            <i class="fas fa-moon"></i>
          </button>
        </div>
      </div>
    </div>
  </nav>

  <div class="container mx-auto p-4 flex-grow">
    <header class="bg-gradient-to-r from-purple-600 to-indigo-600 text-white py-6 rounded-lg shadow-md mb-8">
      <h1 class="text-3xl font-bold text-center">Adicionar Lista de Contatos</h1>
      <p class="text-center mt-2 text-purple-200">Crie e gerencie suas listas de contatos facilmente</p>
    </header>

    <main class="bg-dark-400 p-8 rounded-lg shadow-xl max-w-md mx-auto">
      <form action="/addlist/<%= chave %>" method="POST" class="space-y-6">
        <div>
          <label for="listName" class="block text-sm font-medium text-gray-300 mb-2">Nome da Lista</label>
          <div class="relative">
            <input type="text" id="listName" name="listName" class="block w-full bg-dark-300 border border-dark-200 text-gray-300 py-3 px-4 pr-10 rounded-lg leading-tight focus:outline-none focus:bg-dark-200 focus:border-purple-500" required>
            <div class="absolute inset-y-0 right-0 flex items-center pr-3 pointer-events-none">
              <i class="fas fa-list-ul text-gray-400"></i>
            </div>
          </div>
        </div>
        <button type="submit" class="w-full bg-purple-600 text-white py-3 px-4 rounded-lg shadow hover:bg-purple-700 focus:outline-none focus:ring-4 focus:ring-purple-500 focus:ring-opacity-50 transition ease-in-out duration-150 flex items-center justify-center">
          <i class="fas fa-plus-circle mr-2"></i> Criar Lista
        </button>
      </form>
    </main>


  <script>
    document.getElementById('themeToggle').addEventListener('click', function() {
      document.documentElement.classList.toggle('dark');
    });
  </script>
</body>
</html>