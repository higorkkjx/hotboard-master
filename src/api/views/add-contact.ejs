<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
  <title>Adicionar Contato</title>
</head>
<body class="bg-gray-100 text-gray-800">
  <div class="container mx-auto p-4">
    <header class="bg-blue-500 text-white py-4 rounded-lg shadow-md">
      <h1 class="text-2xl font-bold text-center">Adicionar Contato</h1>
    </header>
    <main class="mt-8 bg-white p-6 rounded-lg shadow-md">
      <form action="/add-contact/<%= chave %>" method="POST" class="space-y-4">
        <div>
          <label for="number" class="block text-sm font-medium text-gray-700">Número</label>
          <input type="text" id="number" name="number" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" required>
        </div>
        <div>
          <label for="listName" class="block text-sm font-medium text-gray-700">Lista</label>
          <select id="listName" name="listName" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm" required>
            <% lists.forEach(list => { %>
              <option value="<%= list.name %>"><%= list.name %></option>
            <% }) %>
          </select>
        </div>
        <button type="submit" class="bg-blue-500 text-white py-2 px-4 rounded">Adicionar</button>
      </form>
    </main>
    <footer class="bg-gray-200 text-gray-600 py-4 mt-8 rounded-lg shadow-md">
      <p class="text-center">Hotboard &copy; 2024</p>
    </footer>
  </div>
</body>
</html>
